VS Code Local Settings Sync 🚀

Простой и легкий инструмент на языке Go для синхронизации настроек Visual Studio Code между машинами в локальной сети. Идеально подходит для обновления конфигурации на изолированных рабочих станциях без доступа в интернет.

📋 Описание

Скрипт позволяет перенести папку User (где лежат settings.json, keybindings.json, сниппеты и т.д.) с "машины-источника" (с интернетом) на "машину-приемник" (без интернета).

Особенности:

✅ Работает без установки дополнительных зависимостей (только стандартная библиотека Go).
📦 Упаковывает настройки в ZIP-архив "на лету" в памяти (без создания мусора на диске).
🔄 Автоматически создает бэкап текущих настроек перед обновлением (с временной меткой).
🌐 Использует HTTP протокол (простота и совместимость с фаерволами).

🔧 Установка и сборка

Для запуска вам не нужно устанавливать Go на целевые машины, только на ту, где вы собираете проект.

Склонируйте репозиторий:
git clone https://github.com/artross/vscode-settings-sync.gitcd vscode-settings-sync
Соберите исполняемый файл под вашу ОС:Для Windows (на PC1/PC2):
go build -o vscode-settings-sync.exe main.go
Для Linux (на PC1/PC2):
go build -o vscode-settings-sync main.go
Полученный файл (vscode-settings-sync.exe или vscode-settings-sync) перенесите на нужные машины.

🚀 Использование

На машине с интернетом (PC1) — Сервер
Запустите скрипт в режиме сервера. Он начнет слушать порт 8080.

# Windows./vscode-settings-sync.exe server# Linux./vscode-settings-sync server
На машине БЕЗ интернета (PC2) — Клиент
Запустите скрипт в режиме клиента, указав IP-адрес PC1.

⚠️ Важно: Перед запуском закройте VS Code на PC2, чтобы избежать конфликтов файлов!

powershell

# Пример (замените 192.168.1.50 на реальный IP PC1)
./vscode-settings-sync client 192.168.1.50
Скрипт скачает архив, переименует вашу текущую папку настроек в User_backup_ГГГГММДД-ЧЧММСС и распакует новые настройки.

⚙️ Конфигурация

По умолчанию используется порт 8080. Если нужно изменить, отредактируйте константу PORT в файле main.go и пересоберите проект.

Скрипт автоматически находит папку настроек в зависимости от ОС:

Windows: %APPDATA%\Code\User
macOS: ~/Library/Application Support/Code/User
Linux: ~/.config/Code/User

📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности смотрите в файле LICENSE.# vscode-settings-sync